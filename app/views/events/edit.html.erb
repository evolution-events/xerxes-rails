<h1><%= t('common.page_header.editing', model: Event.model_name.human).capitalize %></h1>

<%= render partial: 'form', locals: {event: @event} %>

<%= link_to t('common.show'), @event %> |
<%= link_to t('common.back'), events_path %>
