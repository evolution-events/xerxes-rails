<%= render partial: "users/details", locals: { user: user } %>

<p>
  <strong><%= User.human_attribute_name('firstaid') %></strong>
  <%= @user.firstaid %>
</p>

<p>
  <strong><%= User.human_attribute_name('bhv') %></strong>
  <%= @user.bhv %>
</p>

<%= render partial: "users/address", locals: { address: user.address } %>

<%= render partial: "users/medical_details", locals: {user: user} %>

<%= render partial: "users/emergency_contacts", locals: {user: user} %>

