<h1><%= t('common.page_header.editing', model: User.model_name.human).capitalize %></h1>

<%= render partial: 'form', locals: {user: @user} %>

<%= link_to t('common.show'), @user %> |
<%= link_to t('common.back'), users_path %>
